ADMIN_SECRET=vhois-admin-dev
PORT=3001

# Production (Amplify): API Gateway URL from deploy output — no trailing slash
# WAITLIST_API_URL=https://xxxxxxxx.execute-api.ap-south-1.amazonaws.com

# Live transcription + diarization (landing page demo)
# VITE_LIVE_TRANSCRIPTION_WS_URL=wss://api.vhoisai.in/v1/pipeline/stream
# VITE_LIVE_TRANSCRIPTION_API_KEY=your_client_api_key
# Optional — only if forcing a fixed language (default: auto-detect)
# VITE_LIVE_TRANSCRIPTION_LANGUAGE=hi
